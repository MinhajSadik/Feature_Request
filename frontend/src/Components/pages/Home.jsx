import React from "react";

const Home = () => {
  // const { user, isAuth } = useSelector((state) => ({ ...state.user }));
  // console.log(isAuth);
  return <div>HOME</div>;
};

export default Home;
